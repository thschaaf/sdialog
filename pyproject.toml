[build-system]
requires = ["setuptools >= 64.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sdialog"
authors = [
  { name="Sergio Burdisso", email="sergio.burdisso@gmail.com" }
]
maintainers = [
  { name="Sergio Burdisso", email="sergio.burdisso@gmail.com" },
  { name="Severin Baroudi", email="sevbargal@outlook.fr" },
  { name="Yanis Labrak", email="yanis.labrak@univ-avignon.fr" },
]
description = "Synthetic Dialogue Generation and Analysis"
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Intended Audience :: Developers",
    "Intended Audience :: Education",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development",
    "Topic :: Software Development :: Libraries",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICEN[CS]E*"]
dynamic = ["version", "dependencies", "optional-dependencies"]

[project.urls]
Homepage = "https://sdialog.readthedocs.io"
Issues = "https://github.com/idiap/sdialog/issues"
Source = "https://github.com/idiap/sdialog"
Documentation = "https://sdialog.readthedocs.io"

[tool.setuptools.dynamic]
version = {attr = "sdialog.util.__version__"}
dependencies = {file = ["requirements.txt"]}
optional-dependencies = {audio = {file = ["requirements-audio.txt"]}}

[tool.setuptools.packages.find]
where = ["src"]


[tool.setuptools.package-data]
sdialog = ["**/*.yaml", "**/*.j2", "evaluation/dialog2flow/util/visualization/**"]

[tool.pytest.ini_options]
pythonpath = "src/"
norecursedirs = "tutorials/dscaper"
addopts = [
    "--import-mode=importlib",
]
filterwarnings = [
    # Suppress external dependency deprecation warnings
    "ignore:pkg_resources is deprecated:UserWarning",
    "ignore:Deprecated call to.*declare_namespace:DeprecationWarning",
    "ignore:.*LoRACompatibleLinear.*is deprecated:FutureWarning",
    "ignore:.*librosa.core.audio.*Deprecated:FutureWarning", 
    # Suppress Python 3.13 deprecation warnings from audio libraries
    "ignore:'aifc' is deprecated:DeprecationWarning",
    "ignore:'audioop' is deprecated:DeprecationWarning", 
    "ignore:'sunau' is deprecated:DeprecationWarning",
    # Suppress torchaudio backend dispatch warnings
    "ignore:Torchaudio's I/O functions now support par-call bakcend dispatch:UserWarning",
    # Suppress ChatterboxTTS-related warnings
    "ignore:PySoundFile failed.*audioread:UserWarning",
]

[tool.flake8]
max-line-length = 120
